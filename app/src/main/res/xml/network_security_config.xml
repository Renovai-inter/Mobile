<?xml version="1.0" encoding="utf-8"?>
<!--
  Permite tráfego HTTP (sem TLS) apenas para o endereço do emulador/rede
  local, enquanto a API roda em localhost/rede interna durante o
  desenvolvimento. Referencie este arquivo no AndroidManifest.xml:

    <application
        android:networkSecurityConfig="@xml/network_security_config"
        ...>

  Quando a API estiver publicada com HTTPS (produção), remova este arquivo
  e o atributo android:networkSecurityConfig — HTTPS já funciona sem
  configuração extra.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <!-- Se testar em celular físico na mesma rede Wi-Fi, adicione o IP
             da sua máquina aqui, ex: <domain>192.168.0.10</domain> -->
    </domain-config>
</network-security-config>
